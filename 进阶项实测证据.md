# OPC 医院助手 · 进阶项实测证据

> 生成时间：2026-09-22T19:16:33.298Z
> 方式：对本地服务发起真实 HTTP 请求（结果可复现）

## 进阶2 · 交互与结果比较（部分）

| 检查项 | 结果 |
|---|---|
| 手机端适配（viewport + 媒体查询） | ✅ |
| 常用问题入口（快捷 chips） | ✅ |
| 小程序形态模拟页 `/mini.html` | ✅（12671 字节） |
| **条件筛选**（全部 / 仅A级 / A+B级） | ✅ |
| **医院结果对比**（最多 3 家，围绕院区/公开资源/来源可核验性） | ✅ |
| 对比**不以医疗质量排名替代**（已写明） | ✅ |

## 进阶3 · 会话持久化与运行保障

### 缓存（同一问题重复提问）

| 次数 | 状态 | 来源数 | 耗时 | 新增真实检索 | 新增缓存命中 |
|---|---|---|---|---|---|
| 第 1 次 | ok | 16 | 23 ms | 0 | — |
| 第 2 次 | ok | 16 | 17 ms | 0 | 1 |

**判定依据（以计数为准，不看耗时）**：第 2 次查询**新增真实检索 = 0**（应为 0）、**新增缓存命中 = 1**（应 ≥1） → ✅ 缓存生效（第 2 次未消耗检索额度）

> 说明：耗时受首次冷启动与网络波动影响，故用"是否新增检索调用"作为判据更严谨。缓存 TTL 10 分钟、上限 200 条。

### 会话历史查看与重置

- 历史查询：`exists=true`，城市=北京，识别医院 5 家，轮次 4
- 重置后：`exists=false`（✅ 已清除）

### 请求限流

- 连续 25 次请求，触发限流 5 次（阈值：每会话 20 次/分钟）→ ✅ 限流生效

### 调用成本 / 缓存 / 限流指标（`/api/stats`，不含密钥）

```json
{
  "search": {
    "searches": 2,
    "cacheHits": 67,
    "retries": 0,
    "failures": 0,
    "since": "2026-09-22T19:11:29.670Z"
  },
  "cache": {
    "size": 2,
    "ttlMs": 600000,
    "max": 200
  },
  "sessions": 5,
  "rateLimit": {
    "windowMs": 60000,
    "max": 20
  },
  "at": "2026-09-22T19:16:33.800Z"
}
```

### 失败降级方式

- 检索失败 → 返回 `search_failed`，说明限制并建议"医院官网 / 官方挂号入口"核实；
- 无结果 → 返回 `no_result`（或"未查到该院"），**不断言"没有"**；
- 双通道任一失败 → 自动重试一次；仍失败则该通道记为 0 条，另一通道照常出结果（**降级但不中断**）。

### 进阶4 · 小程序实际集成

- ⏳ 未做：需在**自有或已获授权的测试小程序**中完成真实交互；大赛不提供 AppID/账号/生产权限，**非强制项**。
- 已提供 `/mini.html` 作为**小程序形态的模拟调用方**，并明确标注模拟范围。