# OPC 医院助手 · 进阶2「手机端适配」代码级证据

> 生成时间：2026-09-24T09:12:34.416Z
> 生成方式：`node scripts/gen-mobile-evidence.mjs`（BASE=https://opc-hospital-assistant.netlify.app）
> **⚠️ 证据等级声明：本文件是「代码级证据」，不是截图。**
> 本机**没有可用的浏览器**，因此无法生成 375 / 390 / 414px 的真实视觉截图；
> 本文只证明"手机适配相关的代码事实确实存在且可被程序检出"，**不证明视觉呈现效果**。
> **真实手机端视觉截图待人工补齐**（建议补 3 张：首页、结果页、对比表，视口 390×844）。

**检出结果：17/17 项命中**

| 检查项 | 结果 | 证据（程序从页面 HTML 中摘出的原文） |
|---|---|---|
| 网页版 `viewport` 声明（含 `viewport-fit=cover`） | ✅ 命中 | `<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">` |
| 小程序模拟页 `viewport` 声明（含 `viewport-fit=cover`） | ✅ 命中 | `<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">` |
| `width=device-width, initial-scale=1`（禁止横向溢出） | ✅ 命中 | 两项同时具备才判定命中 |
| 网页版窄屏媒体查询 `@media (max-width:560px)` 存在 | ✅ 命中 | 规则原文见下方代码块 |
| 小程序模拟页窄屏媒体查询存在 | ✅ 命中 | 规则原文见下方代码块 |
| 窄屏规则包含"输入框与按钮改为纵向堆叠" | ✅ 命中 | `.row-in{flex-direction:column;align-items:stretch}` |
| 窄屏规则包含"主按钮撑满整行"（拇指可达） | ✅ 命中 | `.btn-main{width:100%}` |
| 窄屏规则包含"标题字号下调" | ✅ 命中 | `h1{font-size:15.5px}` |
| 禁用 iOS 横屏字号自动放大（`-webkit-text-size-adjust:100%`） | ✅ 命中 | -webkit-text-size-adjust:100% |
| `box-sizing:border-box` 全局盒模型（避免窄屏溢出） | ✅ 命中 |  |
| 底部固定栏为内容预留了安全间距（`padding-bottom` ≥ 70px） | ✅ 命中 | .wrap{max-width:880px;margin:0 auto;padding:12px 12px 76px} |
| 常用问题入口（chips）使用 `flex-wrap` 自动换行 | ✅ 命中 | `.chips{display:flex;flex-wrap:wrap;gap:6px}` |
| 结果对比表在窄屏下可读（表格宽度 100% + 允许换行） | ✅ 命中 | `.cmptable{width:100%;border-collapse:collapse;font-size:12.5px;margin-top:6px} .cmptable th,.cmptable td{border:1px solid var(--line);padding:6px 8px;text-align:left;vertical-align:top}` |
| `/mini.html` 可达且是 HTML | ✅ 命中 | HTTP 200，Content-Type=text/html; charset=UTF-8，18785 字节 |
| `/mini.html` 含手机壳容器（模拟小程序形态） | ✅ 命中 | `<div class="phone"` |
| 网页版有入口链到小程序模拟页 | ✅ 命中 | `<a href="/mini.html">` |
| `/api/health` 可达（入口本身可用） | ✅ 命中 | mode=`live`，channels={"bocha":true,"tavily":true} |

---

## 一、窄屏媒体查询规则原文（网页版）

```css
@media (max-width:560px){
    .wrap{padding:10px 10px 72px}
    header{padding:12px 12px 10px}
    header h1{font-size:15.5px}
    .row-in{flex-direction:column;align-items:stretch}
    .btn-main{width:100%}
  }
```

## 二、窄屏媒体查询规则原文（小程序模拟页 `/mini.html`）

```css
@media (max-width:520px){
    body{display:block;background:#fff}
    .phone{width:100%;height:100dvh;border:0;border-radius:0;box-shadow:none}
    .notch{display:none}
  }
```

## 三、复现方式

```bash
# 1) 启动本地服务（或直接用公网入口）
node server.mjs
# 2) 生成本证据文件
node scripts/gen-mobile-evidence.mjs
# 3) 指向公网入口（可选）
BASE=https://opc-hospital-assistant.netlify.app node scripts/gen-mobile-evidence.mjs
```

> 复现判定：脚本会把上面每一行检查项重新跑一遍，**命中就打 ✅、没命中就打 ❌**，
> 不写死结论；如果代码改动导致某项失效，重跑后对应行会立刻变 ❌。

## 四、如实登记：还没做的部分

| 事项 | 状态 | 原因 |
|---|---|---|
| 375 / 390 / 414px 真实视觉截图 | ❌ 未做 | 本机无浏览器，无法截图；**不拿代码级证据冒充截图** |
| 真机（iOS Safari / 微信内置浏览器）实测 | ❌ 未做 | 无可用真机与测试小程序授权 |
| 触摸目标尺寸的像素级测量 | ❌ 未做 | 需浏览器 DevTools，同上 |

