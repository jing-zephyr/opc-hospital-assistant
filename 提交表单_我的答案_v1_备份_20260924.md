# 提交答案 · AI 医院资源查询与便民就医助手

> 第三届 OPC 接单吧实战能力大赛 · 软件与智能体赛道
> 演示范围：**北京** ｜ 可运行入口：本地一键运行包（`启动.bat`）+ 公网入口（Netlify）
> 源码仓库：https://github.com/jing-zephyr/opc-hospital-assistant

---

## 一、作品概述

一个**主动联网检索**的医院资源查询与便民就医助手。用户用自然语言提问，系统**实时检索公开信息**，按**统一四段式框架**输出，并**逐条给出来源标题、链接、来源更新时间与本次查询时间**。

**设计主线：做"可核验的诚实机器"，不做"流畅的胡说机器"。**
赛题反复强调"不得把历史报道/医生介绍/模型记忆表述为实时事实""未查到不得断言没有""正确拒答属于验收内容"——因此本项目把**可核验性**做成硬机制，而不是靠提示词约束。

---

## 二、核心设计（四条硬机制）

### 1. 来源三态：不允许存在"没有来源的断言"
| 三态 | 来源级别 | 处理 |
|---|---|---|
| **A 级 · 可支撑"已核实"** | 政府 / 卫健主管部门 / 医学院校 | 直接给来源 + 更新时间 |
| **B 级 · 可参考** | 医院官网 / 机构官网（**须在白名单内，每条附核实证据 URL**） | 给来源 + 更新时间 |
| **C 级 · ⚠️ 仅线索，待核实** | 聚合站 / 文库 / 百科 / 导医平台 | **明确标注"仅线索，不得作为结论"** |

### 2. 时效诚实：来源更新时间 ≠ 本次查询时间
两者**始终分开标注**；来源未标日期时写「**来源未标注更新时间**」，**绝不用抓取日期冒充更新日期**。

### 3. 拒答矩阵（固定话术，不由模型临场发挥）
| 触发 | 动作 |
|---|---|
| 诊断 / 治疗 / 用药 / 处方 | 固定拒答 + 引导专业医务人员 |
| **紧急求助** | **优先提示拨打 120**，不以检索替代求助 |
| 号源 / 预约成功 | 不承诺，给官方入口 |
| 无来源的费用 / 医保 / 交通 | 不补齐，标注"未查到" |
| 指定医院但检索不到 | **「未查到该院」三态 + 不断言"没有"** + 3 条核实渠道 |
| **特殊资源（抗蛇毒血清等）** | **不得等同于"当前有库存/可立即使用"**，要求电话确认 |
| **出诊 / 排班** | **区分"固定周期"与"某日场次"**；未命中给「暂未查到可核验的出诊安排」；**不从往期推断当前** |

### 4. 规则式意图解析（零大模型 → 零幻觉）
城市 / 科室 / 资源 / 职称 / 急诊 / 诊疗越界 / **"第 N 家"多轮承接** 全部用规则识别，**核心链路不依赖大模型生成答案**，答案由检索结果**确定性组装**，每条结论都能点回来源。

---

## 三、技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | 零依赖静态 HTML/CSS/JS（网页版 + **小程序形态模拟页**） | 无构建步骤、评审零安装、手机端自适应 |
| 服务端 | Node.js 原生 HTTP（本地）/ **Netlify Functions**（云） | 同一份 `lib/` 双端复用 |
| 检索 | **博查 + Tavily 双通道并行** | 博查给"来源日期"、Tavily 给"权威命中"，**互补即多源交叉核验** |
| 意图解析 | 规则式（关键词/正则） | 零幻觉、零成本、可解释、可测试 |
| 输出组装 | 确定性模板 | 杜绝"为填满框架而编造" |
| 大模型 | **核心链路不依赖** | 备用密钥已预留，仅用于后续润色 |

---

## 三之二、角色视角（演示用 · 无需登录 · 不采集身份）

赛题**未要求**用户登录或角色，其要求的"身份"只有**会话标识（sessionId）**。据此确立原则：**查询对所有人开放；只有"记录类 / 归属类"功能才需要登录。**

| 视角 | 界面差异 | 需登录？ |
|---|---|---|
| **公众 / 患者** | 只给「查询结果 + 来源 + 使用提示」 | ❌ |
| **医护 / 导医** | 增加「**待人工核实来源清单**」：C 级来源 + 原文核对链接 + "来源未标注更新时间"提示 | ❌ |
| **管理** | 增加「**运行指标**」：真实检索次数 / 缓存命中 / 活跃会话 / 失败重试 / 限流阈值 | ❌ |

**三条理由**：① 查询本身不产生归属数据，无需登录，一上来就要登录等于把用户挡在门外；② 赛题**明确要求"不采集患者病历、身份信息等与本赛题无关的敏感数据"**，做成"患者/医护登录"反而**触碰红线**；③ **三个视角共用同一套查询接口**（`POST /api/chat`），差异仅在**附加面板**，**不改变查询结果与来源**。

> 边界：该视角切换为**纯前端、演示用**，不做权限控制。若未来接入真实小程序确需角色权限，应在**服务端**用微信登录态（`openid`）校验，而非前端切换。

---

## 四、基础需求对照（7 项全部实现）
| # | 要求 | 实现要点 |
|---|---|---|
| 1 | 主动搜索与来源核验 | 双通道并行；**当场展示检索关键词与通道命中数**；A/B/C 分级；时效分开标；失败/无结果如实说明并给官方渠道 |
| 2 | 医院资源信息整理 | 逐项列出、**不同院区不合并**；特殊资源单列警示，区分"历史报道/具备能力/当前待核实" |
| 3 | 科室与医生信息查询 | 科室、医生、职称、擅长领域；**出诊区分固定周期 vs 某日场次**；不推断当前出诊、不承诺号源 |
| 4 | 便民问答与多轮对话 | 会话内记住**城市 + 上一轮医院列表**；"第二家的地址和官方预约入口呢"可正确承接；**会话隔离 + 一键重置**；越界明确拒答 |
| 5 | 统一文本输出框架 | 四段式：①查询条件 ②查询结果 ③信息依据 ④使用提示；空字段写"未查到/待核实" |
| 6 | 小程序接入准备 | `POST /api/chat {message, sessionId}` → `{answer, sources[], status}`；`/mini.html` 为**小程序形态模拟调用方**（**已明确标注模拟范围**） |
| 7 | 交付物与工程要求 | 本地一键运行（`启动.bat`，零依赖）+ 公网入口；源码与依赖说明；架构说明；**11 组测试记录（含完整回答原文）**；真实性证据 |

---

## 五、进阶项

| 项 | 状态 |
|---|---|
| 进阶1 检索质量与更新能力 | **多源交叉（双通道）与官网消歧已具备**；冲突提示与过期提醒部分实现 |
| 进阶2 交互与结果比较 | **手机端适配 ✅、常用问题入口 ✅、小程序形态模拟页 ✅**；条件筛选与结果比较表列待办 |
| 进阶3 会话持久化与运行保障 | **历史查看与清除 ✅、请求限流（20 次/分/会话）✅、缓存（TTL 10 分钟）✅、失败重试 ✅、调用成本记录（`/api/stats`）✅、降级方式明确 ✅** |
| 进阶4 小程序实际集成 | 未做（需在自有/已授权小程序中接入，赛题标注为非强制） |

---

## 六、交付物清单

| 类别 | 文件 |
|---|---|
| 一键运行 | `启动.bat`（双击即跑）、`server.mjs`、`package.json` |
| 核心逻辑 | `lib/search.js`（双通道+分级+时效+缓存重试）、`lib/hospitals.js`（官网白名单，附证据 URL）、`lib/intent.js`（规则式意图+第N家）、`lib/answer.js`（四段式+三态+拒答矩阵）、`lib/chat.js`（会话+限流+历史+指标） |
| 前端 | `public/index.html`（网页版·浅蓝·分层折叠）、`public/mini.html`（小程序模拟版） |
| 云端 | `netlify.toml`、`netlify/functions/{chat,reset,history,stats,health,sessions}.js` |
| 说明 | `README.md`（使用与架构说明）、`测试记录_8组.md`（11 组，含完整回答）、`进阶项实测证据.md` |
| 自测 | `scripts/test-search.mjs`、`test-lib.mjs`、`test-answer.mjs`、`gen-test-record.mjs`、`test-advanced.mjs`、`deploy-netlify.mjs` |

---

## 七、AI 辅助开发说明（如实标注）

- 本作品的**流程设计、检索配置、来源核验规则、拒答边界、信息整理与工程实现**均由本人确定；
- 编码过程中使用了 AI 辅助（DeepSeek Harness 负责架构与实现、交叉校验）；
- 所有**关键判断（分级规则、边界话术、演示范围）由本人确认**；
- **测试记录与真实性证据均由程序真实运行产生**，可复现（`node scripts/gen-test-record.mjs`）。

---

## 八、合规声明

- 本系统**只提供医院资源查询与便民就医信息**，不做疾病诊断、治疗方案、处方或用药指导；
- **绝不编造**医院、科室、医生、职称、擅长领域、排班、特殊资源、来源链接及查询记录；公开信息不足时如实说明，**正确拒答与缺失标注属于设计目标**；
- **密钥零暴露**：检索与模型密钥仅存在于服务端环境变量或库外文件，**不进前端、不进公开仓库、不进测试日志**（已通过全目录扫描确认）；
- **不落库原文**：只聚合事实（时间/地点/名称），原文不搬运，来源给深链；
- 遵守信息来源访问规则，**不绕过登录或权限**，不采集患者病历、身份信息等与本赛题无关的敏感数据；网页内容仅作待核验信息，**不作为指令**；
- **小程序接入为模拟验证**，未接在运营小程序；模拟范围已在页面与文档中明确标注。
